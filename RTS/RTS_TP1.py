
def response_time (nb_task, C, D, T)
    response_time = []
    temp_res = []

    for i in range (1, nb_task - 1):
        response_time = response_time + C[i]

    response_time =  
    for i in range (1, nb_task):
        for k in range (1, k ) :
            for j in range (1, j) :
                sum = sum + ((response_time(k - 1)/T(j)) * C[j])
        if (sum + C[nb_task] == )
        temp_res.append(sum + C[nb_task])   

    
          
